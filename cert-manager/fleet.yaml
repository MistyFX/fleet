defaultNamespace: cert-manager
helm:
  releaseName: cert-manager
  chart: cert-manager
  repo: https://charts.jetstack.io
  version: 1.12.16
  values:
    installCRDs: true
    webhook:
      enabled: true
    cainjector:
      enabled: true
    startupapicheck:
      enabled: true