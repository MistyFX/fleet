values:
  deployment:
    strategy: Recreate
  config:
    storage:
      type: sqlite
      path: /data/data.db
    ui:
      title: "Status Page"
      description: "Infrastructure Health Monitoring"
